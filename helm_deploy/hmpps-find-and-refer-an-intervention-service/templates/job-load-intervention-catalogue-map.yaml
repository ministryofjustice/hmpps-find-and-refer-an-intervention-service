apiVersion: batch/v1
kind: CronJob
metadata:
  name: load-interventions-catalogue-map
spec:
  schedule: "30 3 * * *"
  suspend: true
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: "Never"
          containers:
            - name: load-interventions-catalogue-map
              image: quay.io/hmpps/hmpps-find-and-refer-an-intervention-service:latest
              imagePullPolicy: Always
              args: ["--jobName=loadInterventionCatalogueMapJob"]
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop:
                    - "ALL"
                runAsNonRoot: true
                seccompProfile:
                  type: RuntimeDefault
{{- include "deployment.envs" . | nindent 14 }}